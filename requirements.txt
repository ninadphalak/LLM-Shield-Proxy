fastapi>=0.100.0
uvicorn>=0.22.0
httpx[http2]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
orjson>=3.9.0
faker>=18.0.0
prometheus-client>=0.17.0
watchdog>=3.0.0
redis>=5.0.0
cryptography>=41.0.0
google-re2>=1.1.20231101
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
PyJWT>=2.13.0

# Restored when pyproject's base dependencies were reduced to the conformance
# harness (httpx only) and the gateway stack moved to the [proxy] extra. Both CI and
# the Dockerfile install `-r requirements.txt` and then a plain `.`, so anything only
# pyproject declared silently disappeared -- the container died on `No module named
# 'cachetools'`. This manifest had in fact been incomplete all along; `pip install .`
# was masking it.
cachetools>=5.3.0   # security/{circuit_breaker,identity,rate_limit}.py - STARTUP path
grpclib>=0.4.3   # api/grpc_service.py, api/ext_proc_pb.py (Envoy ext_proc)
betterproto>=2.0.0b6   # api/ext_proc_pb.py
opentelemetry-exporter-otlp>=1.20.0   # observability/tracing.py
jsonschema>=4.0.0   # not a runtime import; tests/conformance/test_spec_schema.py needs it

