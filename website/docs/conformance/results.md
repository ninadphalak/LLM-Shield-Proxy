# Published Conformance Results

## Published LLM-Shield-Proxy Run

The repository includes a machine-readable v1.0.0 pre-release report at `benchmarks/results/conformance-v1.0.0-pre-release-windows.json`. It covers all six scored domains and includes the required timing distributions. The timing and allocation values apply only to the recorded environment and operations; they are not end-to-end latency or process-wide memory limits.

**Run:** 2026-09-02 on Windows 11, CPython 3.14.7, AMD64; 2,000 timing samples per operation.

**Source label:** `6573c0e60b34203ba4882a78d0da7812ddb514dc+working-tree`

**SHA-256:** `bfb50c51a272ce5150c982e34a6592cd36eb0d9bdf4f5378dc81c902b7c38158`

This result was generated by the project's maintainer on a single machine. No external contributor has replicated it yet.

The `+working-tree` suffix indicates the run included uncommitted source changes. The report tested the `6573c0e...` commit along with local modifications. On every push to `main`, the `Reproducible Public Benchmark` workflow creates a new report tied to the latest commit.

| Domain | Latest Status |
| :--- | :--- |
| Fragmentation safety | Pass - 11 partitions, no reported failures |
| Raw protected-data egress | Pass - email, SSN, and credit-card fixtures absent at tested boundary |
| SSE validity | Pass - valid JSON events, one `[DONE]`, valid termination, split UTF-8 preserved |
| Rehydration fidelity | Pass - exact equality; no placeholder in client-visible output |
| Audit integrity | Pass - two signatures verified; tamper negative control detected |
| Memory bound/measurement completeness | Pass - retained state within bound; allocation recorded; no RSS threshold |

*Latency is reported but not scored. The previous `latency_measurement` check was removed because it only verified non-negative durations.*

### In-Process Timing Observations

| Operation | p50 | p95 | p99 |
| :--- | ---: | ---: | ---: |
| Empty-vault buffer | 0.6 us | 0.7 us | 0.7 us |
| Protected-token buffer | 5.2 us | 5.3 us | 5.8 us |

These figures measure isolated in-process Python operations on a single Windows machine, excluding factors like ASGI, HTTP, TLS, network time, model response time, concurrency, and durable audit I/O. The reported 32-byte peak covers only the declared Python allocation scope, not the entire process RSS. This report supersedes earlier reports generated prior to recent streaming-path optimizations.

## Independent Reproductions

No independent reproductions have been submitted yet. See [Submitting a Result](./submitting) to add your own run.
