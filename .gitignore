# Byte-compiled / optimized / cache files
__pycache__/
*.py[cod]
*$py.class
*.so

# Environments & Packaging
.venv/
env/
venv/
ENV/
build/
dist/
*.egg-info/
.eggs/

# Testing & Coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
*.cover

# Environment Variables & Config
.env
.env.*
!.env.example
/config.yaml
!/config.example.yaml

# Local Redis / Data stores
dump.rdb
*.rdb

# Security & Private Keys
*.asc
*.key
*.gpg
private_key.asc

# IDE / OS files
.DS_Store
.idea/
.vscode/
.swp
.agents/
firebase-debug.log

# LLM/agent scratch context (not for source control)
.llm/
.claude/
repomix-output.xml

# Publication drafts, not yet ready for the public repo
.research/

# Local AI-tool pointer files (never committed)
/AGENTS.md
/GEMINI.md
/CLAUDE.md

# sdist staging directories. `python -m build` leaves `<name>-<version>/` at the repo
# root when it cannot clean up, and one file from it reached a commit that way.
/llm_shield_proxy-*/
/pii_leak_benchmark-*/
